[gd_scene format=3 uid="uid://cfhprpv5h8b6o"]

[ext_resource type="Texture2D" uid="uid://cav3aoalxsxr1" path="res://icon.svg" id="1_8afob"]

[sub_resource type="GDScript" id="GDScript_8afob"]
script/source = "extends CharacterBody2D


const SPEED = 300.0
const JUMP_VELOCITY = -900.0


func _physics_process(delta: float) -> void:
	# Add the gravity.
	if not is_on_floor():
		velocity += get_gravity() * delta

	# Handle jump.
	if Input.is_action_just_pressed(\"ui_accept\") and is_on_floor():
		velocity.y = JUMP_VELOCITY

	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions with custom gameplay actions.
	var direction := Input.get_axis(\"ui_left\", \"ui_right\")
	if direction:
		velocity.x = direction * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)

	move_and_slide()
"

[sub_resource type="RectangleShape2D" id="RectangleShape2D_dovo2"]
size = Vector2(136, 128)

[node name="player" type="CharacterBody2D" unique_id=722074811]
script = SubResource("GDScript_8afob")

[node name="CollisionShape2D" type="CollisionShape2D" parent="." unique_id=1590248227]
shape = SubResource("RectangleShape2D_dovo2")

[node name="Sprite2D" type="Sprite2D" parent="." unique_id=1403313291]
texture = ExtResource("1_8afob")

[node name="Camera2D" type="Camera2D" parent="." unique_id=1614807051]
